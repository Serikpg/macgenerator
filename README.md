# Simple MAC address generator

This simple app is entirely written in simple C. It should serve the purpose for automatically generating simple random MAC addresses.

## Compilation

```
make
```

## Use

```
./magenerator.exe [NR_OF_ADDR]
```
